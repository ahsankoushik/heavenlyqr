
# Postgres
POSTGRES_DB=heavenlyqr
POSTGRES_USER=heavenlyqr
POSTGRES_PASSWORD=heavenlyqr
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379

# Worker
WORKER_CONCURRENCY=4

# Browser-facing URLs — must be reachable from wherever you open the app,
# not just from inside the Docker network.
FRONTEND_ORIGIN=http://localhost:5173
PUBLIC_API_URL=http://localhost:4000
